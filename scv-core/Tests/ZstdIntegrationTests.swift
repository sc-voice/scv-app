import Foundation
import libzstd
import Testing

@testable import scvCore

// Integration tests for Zstd decompression are in scv-build where databases are
// created
struct ZstdIntegrationTests {
  // Tests moved to scv-build/Tests/scvBuildTests/BuildTests.swift
  // where original and compressed databases are available in local/build/
}
